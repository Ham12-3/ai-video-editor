export default function Loading() {
  return (
    <div className="flex items-center justify-center min-h-screen">
      <span className="font-mono text-[12px] tracking-[0.18em] uppercase text-muted-foreground italic">
        Loading…
      </span>
    </div>
  );
}
